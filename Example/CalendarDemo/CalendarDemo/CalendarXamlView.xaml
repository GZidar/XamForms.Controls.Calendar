<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:controls="clr-namespace:XamForms.Controls;assembly=XamForms.Controls.Calendar"
             x:Class="CalendarDemo.CalendarXamlView"
             >
    <ContentView.Content>
        <controls:Calendar 
            Padding="10,0,10,0" 
            ShowNumberOfWeek="false"
            WeekdaysFormat="d1"
            StartDay="Monday"
            CornerRadius="30"
            ShowOutOfMonthDates="false"
            SelectedBackgroundColor="Azure"
            SelectedTextColor="White"
            WeekdaysBackgroundColor="Navy"
            RowAlternateBackgroundColor="LightGray"
            TitleLabelTextColor="Green"
            TitleLeftArrowTextColor="Blue"
            SelectedDate="{Binding Date}"
            SpecialDates="{Binding Attendances}"
            DateCommand="{Binding DateChosen}"
            >
        </controls:Calendar>
    </ContentView.Content>
</ContentView>
